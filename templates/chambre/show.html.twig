{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
     <div class="container mt-4">

        <h1>Chambre</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ chambre.id }}</td>
                </tr>
                <tr>
                    <th>Numero de chambre</th>
                    <td>{{ chambre.numeroChambre }}</td>
                </tr>
                <tr>
                    <th>Nombre de places</th>
                    <td>{{ chambre.nombrePlaces }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_chambre_index') }}" class="btn btn-primary">Retour à la liste</a>

        <a href="{{ path('app_chambre_edit', {'id': chambre.id}) }}" class="btn btn-warning">Modifier</a>

        {{ include('chambre/_delete_form.html.twig') }}
    </div>
{% endblock %}
